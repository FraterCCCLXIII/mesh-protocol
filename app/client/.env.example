# Default: Identity Vault (production-style). Point to your vault service.
# VITE_VAULT_URL=http://localhost:12003

# Optional: full API origin when not using the Vite dev proxy.
# VITE_API_URL=http://localhost:8000

# `vault` (default) or `local` — see docs/IDENTITY.md
# VITE_IDENTITY_MODE=vault
